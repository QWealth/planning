// THROWAWAY. Delete with the rest of harness/.
import { createRoot } from 'react-dom/client';

import RoadmapPage from '../src/pages/RoadmapPage';

createRoot(document.getElementById('root')!).render(<RoadmapPage />);
